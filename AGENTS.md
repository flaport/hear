# Working Agreements

- This is a personal project. After requested work is finished and verified,
  commit the completed changes and push the current branch.
- Work directly on the current branch by default.
- Do not create or use a pull request unless the user explicitly requests that
  a feature be developed in a PR.
- If a PR is explicitly requested, follow the user's stated PR workflow for
  that task.
